# Streamlit Community Cloud: 앱 → ⚙ Settings → Secrets 에 그대로 붙여 넣기
# (이 파일은 예시일 뿐, 실제 키를 여기 적어 커밋하지 마세요.)
# 로컬: `.streamlit/secrets.toml` 또는 루트 `.env` — secrets.toml은 Git 제외

# app.py
OPENAI_API_KEY = "sk-proj-여기에_본인_키"
